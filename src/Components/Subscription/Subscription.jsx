import subCss from "./subscription.module.css";

const Subscription = () => {
  return (
    <div>
      {/* {" "} */}
      <button className={subCss.subBtn}>OK</button>
    </div>
  );
};

export default Subscription;
